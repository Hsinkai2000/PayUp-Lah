<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".PurchasedActivity"
    tools:showIn="@layout/activity_purchased"
    android:orientation="vertical"
    android:weightSum="100">

    <ViewStub
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/stub_list"
        android:inflatedId="@+id/showlayout"
        android:layout_marginTop="10dp"></ViewStub>
    <ViewStub
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/stub_Calendar"
        android:inflatedId="@+id/showlayout"
        android:layout="@layout/activity_expense_planner"
        android:layout_marginTop="10dp"></ViewStub>
</LinearLayout>